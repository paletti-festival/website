name: 💾 Update Files
on:
  workflow_dispatch:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '00 3 * * *'
jobs:
  download-form-style:
    permissions: write-all
    runs-on: ubuntu-latest
    env:
      FILE_URL: https://sibforms.com/forms/end-form/build/sib-styles.css
      TARGET_FILE: assets/styles/_sib-styles.css
      COMMIT_MESSAGE: "Update sib styles"
    steps:
      - uses: actions/checkout@v4

      - name: Download file
        run: |
          curl --fail --location --silent --show-error \
            --output "$TARGET_FILE" \
            "$FILE_URL"

      - name: Commit and push if changed
        run: |
          if git diff --quiet -- "$TARGET_FILE"; then
            echo "No changes."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add "$TARGET_FILE"
          git commit -m "$COMMIT_MESSAGE"
          git push
  # download-form-script:
  #   permissions: write-all
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v7
  #       with:
  #         repository: apriljunge/download-upload
  #         ref: create-commit-only-on-filechange
  #         path: tmp
  #     - name: Install
  #       working-directory: tmp
  #       run: yarn
  #     - name: Build
  #       working-directory: tmp
  #       run: yarn build
  #     - uses: ./tmp/
  #       with:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         url: https://sibforms.com/forms/end-form/build/main.js
  #         dir: scripts
  # download-form-script-license:
  #   permissions: write-all
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v7
  #       with:
  #         repository: apriljunge/download-upload
  #         ref: create-commit-only-on-filechange
  #         path: tmp
  #     - name: Install
  #       working-directory: tmp
  #       run: yarn
  #     - name: Build
  #       working-directory: tmp
  #       run: yarn build
  #     - uses: ./tmp/
  #       with:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         url: https://sibforms.com/forms/end-form/build/main.js.LICENSE.txt
  #         dir: scripts
