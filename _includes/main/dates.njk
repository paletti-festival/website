{% set thisYearDates = collections.dates | thisYear %}
{%- for post in thisYearDates -%}
    {% if loop.first %}
    <h2>{{"dates" | i18n}} {{ currentdate.year}}</h2>
    <div class="biglietti-list">
    {% endif %}
        <div class="biglietti">
            <div class="row">
                <span class="title">{{ '/assets/images/paletti_icon_inverted.svg' | svgContents | safe}} {{ post.data.title }}</span>
            </div>
            <div class="row">
                <div class="date-time">
                    <span class="date">{{ post.data.start | formatDate }}</span>
                    {% if post.data.end %}
                    <span class="time">
                        {{ post.data.start | formatTime }} –
                        {{ post.data.end | formatTime }}
                    </span>
                    {% else %}
                    <span class="time">
                        {{ post.data.start | formatTime }}
                    </span>
                    {% endif %}
                </div>
                <span class="text">
                    {{"free entrance" | i18n}}<br>
                    {{"donation welcome" | i18n}}
                </span>
            </div>
            <div class="row">
                <span class="address">
                    {% if post.data.place %}
                    {{ post.data.place.name }}<br>{{ post.data.place.address }}                
                    {% endif %}
                </span>
            </div>
        </div>
    {% if loop.last %}
    </div>
    {% endif %}
{%- endfor -%}
